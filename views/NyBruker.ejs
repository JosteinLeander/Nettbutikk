<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/nav.ejs') %>

    <div class="main">
      <h2>Lag ny bruker</h2>
      <div class="authForm authInput authLabel">
        <form class="signup" action="/NyBruker" method="post">
          <label for="email">email:</label>
          <input type="email" name="email" required/><br> <br>
          <label for="password">password:</label>
          <input type="passsword" name="password" required/><br> <br>
          <button class="authBtn">Registrer bruker</button>
        </form>
      </div>
      <% if (typeof userError != 'undefined') { %>
        <h2 style="color: red;">Bruker ikke opprettet: <%= userError %></h2>
      <% } %>
      <br />
    </div>

    <%- include('./partials/footer') %>

<!--     <script>
      const signupForm = document.querySelector('.signup');
      signupForm.addEventListener('submit', async (e) => {
        e.preventDefault();

        const email = signupForm.email.value;
        const password = signupForm.password.value;
        console.log(email, password);

        try {
          const res = await fetch('/regBruker', {
            method: 'post',
            body: JSON.stringify({ email, password }),
            headers: { 'Content-Type': 'apllication/json' }
          });
        }
        catch (err) {
          console.log(err);
        }
      });
    </script> -->

  </body>
</html>
